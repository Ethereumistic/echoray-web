import { PhoneMockScroll } from "@/components/sections/phone-mock-scroll"

export default function TestPage() {
    return (
        <div className="min-h-screen flex items-center justify-center bg-background p-8 overflow-x-hidden">
            <PhoneMockScroll />
        </div>
    )
}
